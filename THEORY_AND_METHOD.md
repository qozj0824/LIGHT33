# NØXIS v37.0 계산 원리

이 문서는 작품설명서에 기술된 원리를 프로그램 구현과 대응시켜 정리합니다.

## 1. 전천 영상 좌표 변환

어안영상 픽셀을 방위각/고도로 변환하기 위해 설정 파일의 보정 계수를 사용합니다. 제공된 보고서에 기록된 30개 기준별 회귀 및 독립 검사점 5 px 이하 검증은 **방향별 광해 조회의 좌표 보정 근거**로 취급합니다.

다만 5 px 오차는 각도 RMS와 동일하지 않으므로, 픽셀 입체각을 이용한 절대 표면광도 보정은 별도의 angular RMS 검증 자료가 있을 때만 정량 경로로 활성화합니다.

## 2. 별 제거와 배경광

1. G 채널 또는 선형 단일 채널을 사용합니다.
2. 배경과 robust scatter를 추정합니다.
3. Photutils가 설치되어 있으면 DAOStarFinder로 별 후보를 찾습니다.
4. 별 중심 주변을 원형 마스크로 제외합니다.
5. 각 하늘 영역의 잔여 픽셀에 sigma clipping을 적용합니다.
6. 대표 하늘 배경은 중앙값을 사용하고, 변동성은 robust dispersion/표준오차를 함께 진단합니다.

지상 구조물, 최저 분석 고도 이하, 유효 픽셀 부족, 변동이 과도한 셀은 차폐/저신뢰/결측으로 분리합니다.

## 3. 방향별 전천 배경

하늘을 방위각/고도 격자로 나누고 각 셀의 대표 배경을 저장합니다. 목표 방향은 7.5° 내 good/caution 셀 2개 이상을 우선 보간합니다. 부족하면 15° 내 신뢰 셀(불확실성 최소 15%), 같은 고도대 중앙값(최소 25%), 전천 중앙값(최소 30%) 순으로 대체합니다. 대체 경로는 JSON에 저장되며 정밀 방향값으로 위장하지 않습니다.

## 4. 장비 프로필

망원경 기준 영상의 역할을 매 관측 입력에서 **장비 보정 자료**로 분리했습니다. 한 번 생성한 프로필에는 기준 영상의 실제 측정값이 저장됩니다.

대표 저장값:
- 배경 ADU/pixel/s
- 비포화 별 peak rate(기준 시야의 과거 포화 진단용)
- PSF peak fraction/FWHM
- 기기영점(가능한 경우)
- Csys(동일 조건 전천 기준 영상이 있는 경우)

이는 작품설명서의 망원경 기준 영상 분석을 매 세션 반복하지 않고 같은 장비 조합에서 재사용하도록 구조화한 것입니다. 기준 영상의 촬영시각과 Stellarium 기준 시각이 모두 확인되면 서로 비교하여 잘못된 대기질량·방향 정보를 사용하지 않도록 합니다. 계산 원리는 바꾸지 않습니다.

## 5. 전천→망원경 배경 변환

전천 영상과 망원경 영상은 ADU를 직접 비교하지 않습니다.

전천 기준 방향의 offset 제거 후 단위시간당 배경을

`b_all = (B_all - B0_all) / t_all`

망원경 기준 영상의 단위시간당 배경을

`b_tel = (B_tel - B0_tel) / t_tel`

이라 하면

`Csys = b_tel / b_all`

을 저장합니다.

현재 관측의 목표 방향 전천 배경률을 `b_all,target`이라 하면

`b_tel,target = Csys × b_all,target`

으로 예상 망원경 배경을 계산합니다.

현재 전천 영상의 카메라/ISO·Gain/센서 크롭/Flat 상태가 Csys 기준과 맞지 않으면 Csys를 정량 환산에 사용하지 않습니다.

## 6. 기기영점과 대상 신호

기준 천체의 카탈로그 등급 `m`과 기준 영상의 background-subtracted 전자 신호율 `F_e`를 이용할 수 있으면

`Z = m + 2.5 log10(F_e)`

형태의 기기영점을 저장합니다.

점광원 대상은

`S = 10^(0.4 (Z - m_target))`

형태로 총 전자 신호율을 추정하고, 기준 영상의 대기질량과 현재 대기질량 차이에 대기소광 계수 k를 적용합니다.

확산천체는 표면밝기가 주어지면 이를 우선 사용합니다. 없으면 카탈로그 통합등급과 각크기를 원형으로 근사하여 평균 표면밝기를 추정합니다. 이 경우 실제 구조·필터 대역 차이 때문에 신뢰도는 낮아집니다.

## 7. SNR

한 장의 노출 t에 대한 기본 모델은 천체 신호, 하늘 배경, dark current, read noise를 분리합니다.

`SNR = St / sqrt(St + n_pix(Bt + Dt + R^2))`

여기서 S는 대상의 총 신호율에 맞춰 구현되며, 확산천체에서는 유효 픽셀 수를 별도로 사용합니다.

## 8. 단일 노출 결정

단일 노출은 목표 SNR에 직접 맞추지 않습니다. 먼저 배경·암전류에 대한 읽기잡음 시간상수 `tau = R^2/(B+D)`와 프레임 오버헤드 `o`를 사용해 다음 효율을 계산합니다.

`eff(t) = 1 / ((1 + tau/t) × (1 + o/t))`

무한히 긴 노출의 정보 획득률을 1로 보았을 때 `eff(t) >= 0.90`을 처음 만족하는 실용적인 노출시간을 기본 후보로 사용합니다. 따라서 최대 단일노출을 크게 입력해도 추천값이 그 상한까지 자동으로 증가하지 않습니다. 이후 다음 제한을 적용합니다.

- read-noise 영향이 충분히 낮아지는 sky-limited 하한
- 배경이 센서 가용 범위를 과도하게 차지하기 전 상한
- 선택한 점광원 대상의 PSF 기반 포화 상한
- 확산 대상의 평균 표면밝기와 국소 구조 안전계수에 따른 픽셀 포화 상한
- 기준 시야 대표별 포화시간은 미래 시야의 절대 강제 상한은 아니지만, 효율 후보보다 짧을 때 보수적인 권고 상한으로 사용
- 추적 한계
- 사용자가 지정한 최대 노출

효율 후보와 물리 상한 중 더 짧은 값을 사용하며, 기준 시야 포화 권고가 더 짧으면 그 값을 우선합니다. 결과 JSON에는 효율 목표시간, 추천 시 효율, 실제 hard upper와 선택 근거를 따로 기록합니다.

배경률에 불확실성 `u_B`가 있으면 어두운 경계 `B(1-u_B)`를 읽기잡음 하한에, 밝은 경계 `B(1+u_B)`를 배경·포화 상한에 사용합니다. 신호 모델의 출처(동일 필터 등급, 필터 불일치, 통합등급+각크기, 협대역)에 따라 신호 불확실성을 적용하고 필요 장수 범위를 계산합니다. 이는 특정 결과값을 맞추는 보정이 아니라 모든 입력에 동일하게 적용되는 노이즈·포화 제약식입니다.

v37.0에서는 고정된 심우주 확산 대상에 RA/Dec가 있으면 CDS HiPS2FITS의 DSS2/red cutout을 선택적으로 받아 **상대 밝기 구조**를 측정합니다. 공개 survey의 plate count는 절대 전자율로 변환하지 않습니다. 절대 평균 신호율은 계속 장비 기기영점·대기소광·대상 등급/표면밝기 모델에서 계산하고, survey는 픽셀 간 상대 비율만 제공합니다.

검출된 확산 구조는 밝기 백분위 구역으로 나눕니다. robust 97.5백분위/평균 비율을 `bright_structure_factor`로 두어 대상 픽셀 포화 상한을 계산하고, 검출된 확산 픽셀의 25백분위/평균 비율을 `faint_structure_factor`로 두어 목표 SNR에 필요한 총 적분시간을 계산합니다. 즉 **밝은 구조는 단일노출의 상한을 줄이고, 희미한 구조는 촬영 장수를 늘리며, 희미한 구조 때문에 단일노출 자체를 늘리지는 않습니다.** 이 구분은 코어 포화와 외곽 SNR을 동시에 다루기 위한 핵심 정책입니다.

참조 구조를 얻지 못하거나 신뢰도가 낮으면 기존 평균 표면밝기 방식으로 자동 fallback합니다. 이때 일반 확산천체 1.5, 제한 대상 2.0의 보수적 포화 안전계수를 유지합니다. 계산된 대상 포화 상한이 사용자가 입력한 최소 단일노출보다 짧으면 계획을 `invalid`로 반환하며 허용 가능한 최소노출 범위를 안내합니다.

## 9. 스택 장수

권장 단일 노출에서의 SNR을 `SNR1`, 목표를 `SNRtarget`, 스택 효율을 `eta`라 하면

`N = ceil((SNRtarget / (eta × SNR1))^2)`

로 장수를 추정합니다.

따라서 목표 SNR을 바꾸면 주로 촬영 장수와 총 적분시간이 변하고, 단일 노출은 포화·배경·read-noise 조건이 같으면 크게 변하지 않습니다.

## 9-1. 실제 촬영 데이터 prior

`data/exposure_evidence_summary.json`은 기존 NØXIS Exposure Evidence Dataset의 Target Summary에서 196개 대상의 sub-exposure 분포를 압축한 자료입니다. 대상명(M/NGC/IC)을 우선 매칭하고, 없으면 천체 분류별 p10/p50/p90 요약을 사용합니다. 이 분포는 장비·필터·연구 목적이 서로 다른 아카이브 자료이므로 **물리 계산을 절대 수정하지 않습니다.** 추천값이 경험 분포에서 매우 멀 때 입력 Gain/read noise/포화값/배경 환산을 다시 확인하라는 경고만 추가합니다.

## 10. 신뢰도

다음 조건은 결과를 `planning_only` 또는 낮은 신뢰도로 내립니다.
- Csys가 없거나 현재 전천 조건과 일치하지 않음
- 기기영점이 없음
- 확산천체의 표면밝기를 통합등급+각크기로 근사
- 전천 Flat이 없음
- 대상 방향 셀 신뢰도가 낮음
- 센서 clipping 근거가 없음
- 렌더링된 JPEG/PNG/TIFF 사용

프로그램은 이러한 경우 숫자를 숨기거나 경고와 함께 관측 계획 시작값으로만 제공합니다.


## 11. 장비 프로필 재사용의 범위

장비 프로필은 광학계·센서 설정처럼 반복 사용 가능한 특성을 저장하는 장치입니다. 기준 사진에 들어 있던 **특정 시야의 천체 분포 자체**를 장비 특성으로 간주하지 않습니다. 따라서 과거 기준 사진의 대표 밝은 별은 포화 진단에만 사용하고, 현재 점광원 대상의 포화는 현재 카탈로그 광도와 저장된 PSF peak fraction으로 별도 계산합니다. 확산천체나 오늘 시야의 다른 밝은 별은 카탈로그 통합등급만으로 국소 peak를 확정할 수 없으므로 결과에 한계를 명시합니다.

협대역/다중대역 필터에서는 V등급이 해당 필터 통과대역의 광자율을 직접 나타내지 않으므로, V등급 기반 절대 SNR·장수 예측은 자동으로 관측 계획용 근사로 낮춥니다.
