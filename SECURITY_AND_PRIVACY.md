# 보안 및 개인정보

LIGHTT v34는 기본적으로 `127.0.0.1` 로컬 서버에서 실행하도록 구성되어 있습니다.

- 업로드된 임시 분석 파일은 프로그램 폴더의 `uploads/`에 저장되고 정리 정책에 따라 삭제됩니다.
- 분석 결과는 `results/`에 저장됩니다.
- 장비 프로필은 `profiles/<profile-id>/` 아래에 기준 망원경 영상, 선택적 기준 전천 영상, 측정 JSON을 저장합니다. 프로필을 삭제하면 해당 폴더도 삭제됩니다.
- `profiles/`는 FastAPI의 정적 파일 경로로 공개하지 않습니다.
- Stellarium 연결은 localhost 또는 사설 IP 범위만 허용하는 방어 로직을 사용합니다.
- 프로그램은 클라우드 업로드를 요구하지 않습니다.

LAN 전체에 공개하려고 `--host 0.0.0.0`으로 실행하면 로컬 전용 전제보다 공격 표면이 커집니다. 특별한 이유가 없다면 기본 `127.0.0.1` 실행을 유지하십시오.
