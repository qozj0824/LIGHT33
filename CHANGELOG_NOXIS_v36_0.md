# NØXIS v36.0 변경 내역

## 노출 계획

- 최대 단일노출 110초가 45초로 축소되던 세션 플래너의 `min(lower, upper)` 선택 오류를 수정했습니다.
- 물리 안전비율이 이미 적용된 실제 상한 중 가장 짧은 값을 권장 단일노출로 선택합니다.
- 결과 JSON에 요청받은 최소/최대/추적/포화/배경/스택 조건, 선택 근거, 하한 충돌 상태를 저장합니다.
- 최대 장수 초과 시 필요한 전체 장수와 최대 장수에서 달성 가능한 SNR을 남깁니다.

## 데이터 독립 자동 판정

- 미등록 카메라에 Canon/Sigma 보정을 자동 적용하지 않습니다.
- 영상 증거가 있을 때만 원형 어안 중심·반경을 추정합니다.
- 북쪽 방향을 검증할 수 없으면 목표 방향을 추측하지 않고 전천 중앙 배경으로 대체합니다.
- 외곽 pedestal은 카메라 이름이 아니라 원형 밖 표본 수, 균일도, 내부 대비를 모두 통과할 때만 적용합니다.
- 저해상도 영상은 유효 픽셀 수에 맞춰 하늘 격자를 자동 축소합니다.

## 검증과 복구

- 업로드 단계에서 선형성, 노출시간, 촬영시각, 카메라 식별, 어안 형상과 자동 복구 경로를 판정합니다.
- Csys 기준/현재 영상의 원본 형식, dtype, bit depth, Gain, 크롭, Flat 조건을 교차 확인합니다.
- 고정식 카메라 보정 관측소와 Stellarium 위치가 다르면 분석을 차단합니다.
- 오래되거나 브라우저에서 복구된 장비 프로필의 필수 센서 값을 검증하고 잘못된 선택 값은 안전하게 비활성화합니다.
- 예기치 않은 서버 오류에는 Render 로그와 연결되는 오류 ID를 표시합니다.

## 과학적 보수성

- Csys 품질, 전천 Flat, offset, 방향 보간 오차에 따라 보수적인 배경 불확실성 여유를 SNR/상한 계산에 반영합니다.
- I/B/R 등 V가 아닌 필터에서 V등급을 쓴 경우 색항 불일치를 자동 표시합니다.

## 검증

- 자동 테스트 89개 통과.
- Python compileall, Mypy, Ruff 통과.
