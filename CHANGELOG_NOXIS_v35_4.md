# NØXIS v35.4

이번 패치는 합성 전천영상 검증 중 실제로 드러난 작은 사용성/안정성 문제를 정리한 버전입니다.

## 수정 사항

- **Render 재시작 후 장비 프로필 자동 복구**
  - 장비 프로필 전체 계산 결과를 브라우저 `localStorage`에도 백업합니다.
  - Render의 임시 파일시스템에서 `profiles/`가 사라져도 분석 요청에 브라우저 백업본을 자동 첨부합니다.
  - 기존 서버 프로필도 페이지를 한 번 열면 자동으로 브라우저 백업합니다.
- **전천영상 임시 토큰 복구 유지**
  - 서버 재시작으로 미리보기 토큰이 만료되면 이미 선택된 원본 파일을 자동 재업로드하는 기존 복구 동작과 프로필 복구를 함께 사용합니다.
- **Stellarium 재연결 개선**
  - 일시적인 localhost/Render 오류가 나도 마지막으로 정상 수신한 천체를 화면에서 지우지 않습니다.
  - 연결이 복구될 때까지 분석 버튼만 잠그고 6초 주기의 자동 동기화를 계속합니다.
  - NØXIS 서버 오류와 Stellarium Remote Control 오류 메시지를 구분합니다.
  - 동기화 요청이 겹치지 않도록 in-flight 가드를 추가했습니다.
- **ADU 그래프 축 표시 수정**
  - Matplotlib의 `+2.2e3` offset 표기를 끄고 `2199.8, 2200.0, 2200.2`처럼 실제 ADU를 그대로 표시합니다.
- **Render 한글 글리프 경고 제거**
  - 한글 폰트가 없는 서버에서는 셀 신뢰도 컬러바도 영어 라벨로 자동 전환합니다.
- **메모리 회수 강화**
  - 전천/기준영상 inspect, 프로필 생성, 관측 분석 후 GC 및 Linux `malloc_trim`을 수행합니다.
  - inspect 성공 직전 전체 해상도 이미지 배열 참조를 명시적으로 해제합니다.
  - Render 재시작 자체를 완전히 금지할 수는 없지만 반복 분석 시 메모리 압박을 줄이도록 보강했습니다.
- **진단 정보 추가**
  - `/health`에 서버 instance id, PID, uptime을 표시합니다.
  - 서버가 재시작되어 브라우저 프로필을 사용한 분석은 결과 JSON에 복구 사실을 기록합니다.

## 검증

- pytest: **56 passed / 4 skipped**
  - skip 4개는 현재 패키징 검사 환경에 `astropy`가 설치되지 않아 생긴 선택적 테스트 skip입니다.
- 서버 smoke test: **PASS**
- 합성 `S02_bright_uniform.png`: **2200.0 ADU** 재현 확인
- 그래프의 scientific/offset 축 표기 제거 확인
- Korean glyph warning: 합성 S02 sky-map 생성 기준 **0건**
