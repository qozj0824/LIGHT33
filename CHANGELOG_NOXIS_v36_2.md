# NØXIS v36.2 변경 내역

## 결과 표시

- 권장 단일노출은 분/시간으로 바꾸지 않고 항상 초 단위로 표시합니다.
- 총 적분시간은 기존처럼 분/시간 요약을 유지합니다.
- 배경·신호 불확실성을 권장 노출 범위와 필요 장수 범위로 보존합니다.

## 메타데이터

- FITS `EXPTIME`, `EXPOSURE`, `EXP_TIME`, `ONTIME`, `LIVETIME`, `TOTEXP`, `DIT`, `NDIT` 계열과 명시적 ms/us 키를 후보로 분석합니다.
- ESO 검출기 영상은 필요할 때 `DIT×NDIT`로 영상 적분시간을 유도합니다.
- 후보 간 2% 이상 불일치를 숨기지 않고 헤더 충돌과 선택 근거를 JSON/UI에 남깁니다.
- EXIF 유리수 노출시간과 RAW shutter 메타데이터의 출처를 저장합니다.

## 계산 과정

- 세션/일반 분석이 공통 `RN²/(B+D)` 시간상수와 프레임 오버헤드 정보효율식을 사용합니다.
- 배경 불확실성의 어두운 경계는 읽기잡음 제약에, 밝은 경계는 배경/포화 제약에 적용합니다.
- 필터 불일치·협대역·통합등급+각크기 근사는 신호 불확실성과 장수 범위로 전파됩니다.
- 추적 상한 0은 무제한 추적을 입증하지 않으며, 추적 제약 미입력 경고를 남깁니다.

## 전천 배경 회복

- 엄격 방향 보간 실패 시 15° 내 신뢰 셀, 같은 고도대, 전천 중앙값 순으로 대체합니다.
- 대체 단계마다 최소 15/25/30% 불확실성을 부여하고 `planning_only`로 낮춥니다.
- 동일 입력이 근처 셀 결측으로 재시도할 때마다 422가 나는 경로를 제거했습니다.

## UI

- 글꼴 설치 여부와 관계없이 동일한 동글동글한 SVG 워드마크를 사용합니다.
- `Ø`는 중앙점 없는 흰색 행성 링과 파스텔 색의 앞/뒤 토성 고리로 교체했습니다.

## 검증

- 99개 자동 테스트 통과.
- FITS 표준/먀리초/DIT×NDIT/충돌/평균 스택과 EXIF 유리수 회귀 테스트 추가.
- 목표 셀 결측 대체 경로와 불확실성 하한 회귀 테스트 추가.
