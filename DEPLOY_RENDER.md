# LIGHTT v35.0 Web — Render 배포 안내

이 폴더는 LIGHTT v34.2의 계산 원리를 유지하면서 **Render 무료 Web Service**에 올릴 수 있도록 수정한 웹 배포판입니다.

## 핵심 변경

- 장비 프로필은 Render 서버 디스크가 아니라 **사용자 브라우저 localStorage**에 저장됩니다.
- 장비 프로필 생성에 사용한 RAW/FITS 원본은 처리 후 서버 임시 폴더에서 제거됩니다.
- 장비 프로필은 JSON으로 내보내고 다시 가져올 수 있습니다.
- Render 서버가 사용자의 `127.0.0.1` Stellarium에 접근할 수 없으므로, **브라우저가 Stellarium Remote Control API에 직접 연결**합니다.
- Stellarium에서 가져온 JSON만 LIGHTT 서버에 보내 좌표/천체 정보를 정규화합니다.
- 무료 서버의 512 MB RAM을 고려해 동시 분석을 1개로 제한하고 업로드/픽셀 제한을 보수적으로 설정했습니다.

## 1. GitHub에 올리기

새 GitHub 저장소를 만든 뒤 이 폴더의 **내용물 전체**를 저장소 루트에 올립니다. `render.yaml`이 저장소 최상위에 있어야 합니다.

## 2. Render에서 배포

1. Render에 로그인합니다.
2. `New +` → `Blueprint`를 선택합니다.
3. 위 GitHub 저장소를 연결합니다.
4. Render가 루트의 `render.yaml`을 읽으면 `lightt-observation-planner` Web Service가 표시됩니다.
5. `Deploy Blueprint`를 누릅니다.
6. 빌드가 끝나면 `https://<서비스이름>.onrender.com` 주소가 생성됩니다.

`render.yaml`은 한국 사용자의 지연시간을 줄이기 위해 `singapore` 지역을 사용합니다.

## 3. Stellarium Remote Control 설정

웹 배포판에서는 Stellarium이 **사용자 PC**에서 실행되므로 다음 설정이 필요합니다.

1. Stellarium → 플러그인 → Remote Control 활성화
2. 서버 시작, 기본 포트 `8090`
3. CORS 활성화
4. CORS 허용 Origin에 배포된 LIGHTT 주소를 정확히 입력합니다.
   - 예: `https://lightt-observation-planner.onrender.com`
   - 초기 테스트에서만 `*`를 사용할 수 있지만 보안상 최종 사용에서는 정확한 Origin을 권장합니다.
5. LIGHTT에서 주소를 `http://127.0.0.1:8090`으로 두고 `연결 확인`을 누릅니다.

Chrome/Firefox 계열 브라우저를 권장합니다. 브라우저가 로컬 네트워크/루프백 접근 권한을 묻는 경우 허용해야 Stellarium 연동이 동작합니다.

## 4. 장비 프로필 저장 방식

Render 무료 Web Service는 로컬 파일이 영구 저장되지 않습니다. 그래서 이 버전은 장비 프로필을 브라우저에 저장합니다.

- 같은 브라우저/같은 사이트 주소에서는 재접속 후에도 프로필을 사용할 수 있습니다.
- 브라우저 사이트 데이터 삭제, 시크릿 모드, 다른 PC에서는 자동으로 옮겨지지 않습니다.
- `선택 프로필 내보내기`로 JSON 파일을 보관해 두는 것을 권장합니다.
- 다른 PC에서는 `프로필 JSON 가져오기`로 복원할 수 있습니다.

## 5. Render 무료 플랜의 현실적인 제한

Render Free Web Service는 512 MB RAM / 0.1 CPU이며 15분간 요청이 없으면 절전됩니다. 첫 접속 시 다시 켜지는 데 약 1분이 걸릴 수 있습니다. 무료 인스턴스의 로컬 파일은 재시작/절전/재배포 시 사라집니다.

LIGHTT는 RAW/FITS를 Python에서 분석하기 때문에 무료 인스턴스에서는 로컬 PC보다 느립니다. 이 배포판은 다음 제한을 기본 적용합니다.

- 파일 1개: 최대 약 180 MiB
- 요청 전체: 최대 약 260 MiB
- 원본 영상: 최대 45 MP
- 동시 분석: 1개

30 MP급 전천 RAW는 RAW Green plane이 내부적으로 절반 해상도로 해석되고, 방향별 하늘지도 계산은 최대 약 1200 px 축으로 축소하여 수행하도록 기존 계산 엔진이 구성되어 있습니다. 그래도 보정 프레임을 여러 장 동시에 올리거나 큰 FITS를 반복 처리하면 512 MB 한계에 걸릴 수 있습니다.

과학전람회 시연처럼 안정성이 중요하면 Render의 2 GB 이상 인스턴스나 로컬 실행판을 사용하는 것이 더 안전합니다.

## 6. 무료 서버가 절전된 뒤 미리보기 토큰이 사라지는 경우

웹 배포판은 최종 분석 요청에 원본 전천 영상도 다시 포함합니다. 따라서 미리보기 후 서버가 절전되어 임시 토큰이 사라졌더라도, 최종 분석 시 원본 파일을 다시 업로드해 자동 복구하도록 수정했습니다.

## 7. 개인정보/파일 처리

업로드한 천체 영상은 분석을 위해 Render 서버의 임시 파일시스템에 저장됩니다. 분석 작업 폴더는 처리 후 제거되며, 무료 서버 재시작 시 임시 파일시스템 자체도 초기화됩니다. 장비 프로필에는 계산된 수치와 메타데이터만 브라우저에 저장됩니다.

공개 사이트이므로 개인 식별 정보가 포함된 파일명, FITS 헤더, 위치 정보 등을 업로드할 때는 사용자가 직접 확인해야 합니다.
