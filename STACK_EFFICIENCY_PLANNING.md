# NØXIS v38.0 — 스택 효율 및 한계효용 기반 총 적분 계획

## 목적
과거 방식은 사용자가 정한 목표 SNR을 달성할 때까지 `sqrt(N)`을 역산했기 때문에 매우 희미한 천체에서 수십 시간의 비현실적 권고가 나올 수 있었습니다. v38.0은 목표 SNR을 메인 세션의 총 적분 결정 변수에서 제거하고, 현실적인 시간 범위 안에서 추가 촬영의 정보 이득이 얼마나 감소하는지 계산합니다.

## 단일노출과 총 적분의 분리
단일노출은 기존 물리 모델을 유지합니다. read noise, 하늘 배경, dark current, 프레임 오버헤드, 대상/배경 포화, 추적 한계, 사용자 최대 단일노출을 사용합니다. 스택 효율 모델은 이 값을 억지로 늘리지 않습니다.

총 적분은 권장 단일노출을 고정한 뒤 프레임 수 N을 늘려가며 계산합니다.

`SNR_N = eta × SNR_1 × sqrt(N)`

천체 구조 모델을 신뢰할 수 있으면 P0–20부터 P95–100까지 각 밝기 구역에서 이 계산을 독립적으로 수행합니다.

## 구조 정보효용
각 구역의 SNR을 다음 부드러운 효용으로 바꿉니다.

`U(SNR) = 1 - exp(-SNR / 5)`

5는 목표값이 아니라 효용 곡선의 스케일입니다. 각 구역 U는 해당 구역의 픽셀 비율로 가중 평균해 전체 `structure_utility`를 만듭니다. 동시에 SNR≥3, SNR≥5, SNR≥10인 구조 픽셀 비율을 별도로 보고합니다.

## 한계효용 기반 4개 촬영 모드
총 경과시간의 로그에 대한 structure utility 증가 기울기를 계산하고 가볍게 평활화합니다. 정보 획득 기울기가 가장 큰 지점을 `빠름` 기준으로 삼고, 그 이후 기울기가 최고점의 다음 비율 이하가 되는 첫 지점을 권고합니다.

- 빠름: 최고 효율점
- 균형: 65%
- 고품질: 35%
- 매우 깊게: 18%

이 숫자는 자연상수가 아니라 동일한 기준으로 촬영시간의 수확 체감을 비교하기 위한 정책값입니다.

## 유한 계획범위
기본 계획범위는 12시간이며 사용자가 변경할 수 있습니다. 그 범위 끝까지 한계효용 기준에 도달하지 못하면 추천을 수십 시간 앞으로 외삽하지 않고 현재 범위 끝을 반환하며 `horizon_limited=true`로 표시합니다.

## 불확실성
기존 신호/배경 불확실성을 낙관·비관 경계에 적용해 동일한 스택 효율 계산을 다시 수행하고, 추천 장수와 총 적분시간의 범위를 제공합니다. 이는 통계적 보증구간이 아니라 입력 모델 불확실성의 민감도 범위입니다.
