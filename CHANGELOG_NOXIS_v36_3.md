# NØXIS v36.3 변경 내역

## FITS 헤더와 속도

- 전체 FITS 헤더를 Astropy WCS에 넘기지 않고 표준 천구 WCS 카드만 추려 처리합니다.
- ESO `HIERARCH` 관측소 카드 수백 개를 검증·수정하느라 수분이 걸리던 경로를 제거했습니다.
- 큰 영상의 미리보기 통계는 결정론적 표본을 사용하고, 센서 clipping 최소·최대는 전체 프레임을 유지합니다.
- `/api/inspect` 응답과 로그에 업로드·디코드/헤더·후처리·전체 시간을 기록합니다.

## 업로드와 미리보기

- 메인 전천 업로드가 검사 완료 전에 `노출시간을 읽지 못했습니다`라고 표시하던 상태 경합을 수정했습니다.
- 빠른 검사가 실패해도 업로드 토큰을 폐기하지 않고 본 분석에서 원본 메타데이터를 다시 읽습니다.
- 새 장비 프로필 생성 시 망원경/전천 PNG 미리보기를 즉시 만들고, 축소 JPEG 사본을 브라우저 프로필에도 보관합니다.
- 서버 프로필을 브라우저에 복구하는 작업이 화면 표시를 막지 않도록 비동기로 전환했습니다.

## 밝은 대상 계산

- 확산 대상의 평균 전자율/pixel과 국소 구조 안전계수로 대상 자체의 포화 상한을 계산합니다.
- 행성·달·혜성에는 더 보수적인 구조계수를 사용합니다.
- 대상 포화 상한이 사용자 최소 단일노출보다 짧으면 500/422 대신 설명 가능한 `invalid` 계획을 반환합니다.

## 검증

- 자동 테스트 101개 통과.
- 4096×4096 APICAM FITS의 120초 헤더와 PNG 미리보기를 실제 HTTP 경로에서 확인했습니다.
- ESO 카드 300개를 포함한 FORS2 FITS의 110.0004초 헤더, WCS 중심, PNG 미리보기를 확인했습니다.
