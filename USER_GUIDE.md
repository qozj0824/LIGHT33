# NØXIS v38.0 사용 안내

## 1. 처음 사용할 때: 장비 프로필 생성

상단의 `장비 프로필 관리`를 엽니다.

### 필수 입력
- 망원경 기준 RAW/FITS 한 장
- 프로필 이름
- 실제 조합을 구분할 수 있는 망원경/카메라/필터/Gain·ISO/binning 정보
- 변환 Gain(e-/ADU), read noise 등 알고 있는 센서 물성
- 기준 영상의 노출시간(헤더에서 읽지 못할 때 직접 입력)

기준 망원경 영상은 오늘 촬영할 대상일 필요가 없습니다. 동일 장비 조합의 대표 사진이면 됩니다.

### Csys를 정량 보정하려면
가능하면 기준 망원경 영상을 촬영한 시각·장소에 가까운 **기준 전천 영상**도 한 번 함께 입력하십시오. 해당 전천 영상의 목표 방향 배경과 망원경 영상 배경을 연결해 장비 변환계수 Csys를 저장합니다.

기준 전천 영상이 없더라도 프로필 생성은 가능하지만, 이후 전천→망원경 배경 환산은 상대배경 근사로 처리되어 결과 신뢰도가 낮아집니다.

### 권장: 기준 천체
기준 영상의 주대상을 알고 있다면 Stellarium에서 선택해 가져오는 것을 권장합니다. V등급이 있으면 기기영점 계산에 사용합니다. 기준 영상과 Stellarium 시각이 달라도 프로필 생성은 중단하지 않습니다. 프로그램은 기준 영상의 DATE-OBS/RAW 촬영시각을 자동 읽고, RA/Dec와 관측 위치가 있으면 그 촬영시각의 고도·방위각을 다시 계산합니다. 재계산할 정보가 부족하면 Csys·기준 대기질량처럼 시간에 의존하는 항목만 비활성화합니다. 화면의 `Stellarium을 기준 촬영시각으로 맞춤` 버튼을 사용하면 기준 천체 확인을 위해 Stellarium을 과거 촬영시각으로 이동시킬 수도 있습니다.

확산천체 사진도 등록할 수 있지만, 자동 ROI가 천체의 전체 통합광을 완전히 포함한다고 보장하기 어렵기 때문에 기기영점은 `approximate`로 취급될 수 있습니다. 점광원 또는 광도 정보가 명확한 소형 천체를 기준으로 쓰면 더 안정적입니다.

## 2. 실제 관측일

### 2-1. 현재 전천 영상
관측 직전의 전천 RAW/FITS를 입력합니다. 프로그램이 즉시 미리보기와 메타데이터를 표시합니다.

큰 FITS는 먼저 `헤더·미리보기 읽는 중`으로 표시됩니다. 이 상태에서는 노출시간 누락으로 판정하지 않습니다. 빠른 미리보기 검사가 실패해도 원본 업로드는 유지되며, 분석을 누르면 같은 메타데이터 판독기를 사용해 원본 파일을 다시 읽습니다. FITS에서는 `EXPTIME`, `EXPOSURE`, `DIT×NDIT` 등 후보와 선택 근거가 결과 JSON에 남습니다.

가능하면 장비 프로필에서 Csys를 만들 때 사용한 것과 같은 전천 카메라, Gain/ISO, 크롭, Flat 처리 조건을 유지하십시오.

### 2-2. 관측 대상
Stellarium에서 오늘 촬영할 천체를 선택한 뒤 `Stellarium 선택 천체 가져오기`를 누릅니다.

프로그램은 가능한 범위에서 다음을 자동으로 사용합니다. 전천 영상에 촬영시각이 기록되어 있으면 Stellarium 기준 시각과도 비교하며, 30분을 초과하면 천체의 Alt/Az가 해당 전천 영상의 시점과 맞지 않는 것으로 판단해 분석을 중단합니다.
- 천체명
- 종류
- RA/Dec
- 현재 Alt/Az
- V등급 또는 대기소광 후 등급
- 각크기

고도 0° 미만이면 현재 관측 불가로 처리합니다. 최저 분석 고도(기본 15°)보다 낮으면 광해지도 조회와 촬영 계획을 중단합니다.

### 2-3. 장비 프로필
오늘 사용하는 망원경·카메라·필터·Gain 조합과 일치하는 프로필을 선택합니다.

### 2-4. 스택 품질과 계획 범위
메인 세션에서는 목표 SNR을 입력하지 않습니다. `빠름 / 균형 / 고품질 / 매우 깊게` 중 촬영 목적을 고르면 NØXIS가 밝기 구역별 SNR-시간 곡선과 추가 촬영의 한계효용을 계산합니다. 고급 설정의 `스택 계획 최대 시간`은 알고리즘이 검토할 현실적인 시간 범위이며 기본값은 12시간입니다. 이 범위 끝까지 효율이 유지되면 NØXIS는 더 긴 시간을 억지로 외삽하지 않고 `계획범위 제한`으로 표시합니다.

## 3. 결과 해석

### 핵심 결과
- 권장 단일 노출
- 한 장 예상 SNR
- 스택 효율 기반 추천 촬영 장수
- 추천 총 적분시간과 실제 경과시간(프레임 오버헤드 포함)
- 대상 고도
- 예상 하늘 배경

### 상세 진단
- 현재 방향의 전천 배경 ADU
- Csys 또는 상대배경 환산 방식
- 천체 신호 모델
- sky-limited 하한
- background/포화/추적 상한
- 결과 신뢰도 저하 원인

`관측 계획용`으로 표시된 값은 사용 가능한 시작값이지만 절대적인 최적값을 의미하지 않습니다.

## 4. 장비 설정을 바꾼 경우
다음 항목 중 하나라도 크게 바뀌면 새 장비 프로필을 만드는 것을 권장합니다.
- 카메라
- 망원경/리듀서
- 필터
- Gain/ISO
- binning
- 센서 readout mode

## 5. 프로필 삭제
`장비 프로필 관리`에서 삭제할 프로필을 선택한 뒤 `선택 프로필 삭제`를 누릅니다. 오삭제 방지를 위해 5초 이내에 같은 버튼을 한 번 더 눌러야 실제 삭제됩니다.

## 6. 저장 프로필 미리보기

v38.0에서는 계산에 필요한 장비 프로필 핵심 JSON과 미리보기 이미지를 브라우저에서 분리해 저장합니다. 미리보기가 커져 저장공간을 차지해도 핵심 장비값 백업이 영향을 받지 않도록 했습니다. Render 재시작/sleep으로 서버의 `profiles/`가 사라진 경우 같은 브라우저의 핵심 프로필을 자동 검증한 뒤 현재 Render 인스턴스에 다시 생성합니다. `JSON 내보내기/가져오기`로 브라우저 데이터 삭제나 다른 기기 이동에도 대비할 수 있습니다. 원본 RAW/FITS 자체는 브라우저 백업 대상이 아니므로 이미 사라진 원본은 자동 복구할 수 없습니다.

## v38.0 천체 내부 밝기 구조와 스택 효율

고정 심우주 확산 대상의 RA/Dec가 있으면 분석 중 CDS DSS2 참조 영상을 작은 FITS cutout으로 조회합니다. 결과 개요의 **천체 밝기 구역** 그림에서 대상의 상대 밝기 분포를 확인할 수 있습니다.

- 밝은 구조 계수: 대상 자체의 포화 상한 계산에 사용
- P0–20부터 P95–100까지 6개 밝기 구역: 시간에 따른 구역별 스택 SNR과 구조 확보율 계산에 사용
- 희미한 구조: 단일노출을 늘리지 않고 추가 스택의 가치 평가에 반영
- 공개 survey ADU: 절대 광량으로 사용하지 않음
- 조회 실패/신뢰도 낮음: 평균 신호 기반 스택 효율 계산으로 자동 fallback
- 실제 촬영 데이터 prior: 이상값 경고용이며 추천값을 변경하지 않음

결과 JSON의 `target_structure_model`, `plan.stack_efficiency_plan`, `exposure_evidence_prior`에서 세부 근거를 확인할 수 있습니다. `stack_efficiency_curve.png`는 총 적분시간에 따른 구조 정보효용과 SNR≥5 구조 비율을 시각화합니다.
