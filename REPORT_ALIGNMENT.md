# 작품설명서 ↔ NØXIS v35.1 구현 정합표

## 보고서의 핵심 처리와 구현

| 작품설명서 원리 | v34.2 구현 |
|---|---|
| 어안 영상 좌표 보정 | `lightt/geometry.py`, `config/fisheye.json` |
| DAOStarFinder 기반 별 검출 | `lightt/sky.py`, Photutils 설치 시 기본 경로 |
| 별 주변 원형 마스크 | `lightt/sky.py`, `lightt/photometry.py` |
| sigma clipping + 대표 배경 | `lightt/sky.py`, `lightt/photometry.py` |
| 방위각·고도 광해 지도 | 극좌표/직사각형/상대배경/신뢰도 지도 |
| 전천 ADU를 망원경 조건으로 환산 | 장비 프로필의 Csys |
| 망원경 기준 영상의 대상/배경 신호 분석 | 장비 프로필 생성 시 1회 수행 |
| 대기질량·대기소광 | `lightt/equipment.py`, `lightt/session.py` |
| SNR | 대상·배경·dark·read-noise 분리 모델 |
| 포화 상한 | 센서 clip + 현재 점광원 대상 PSF peak; 기준 시야 대표별 peak는 진단값으로만 보존 |
| 목표 SNR과 촬영 장수 | 단일 노출과 분리, sqrt(N) 기반 스택 |
| 신뢰도·경고 | quantitative_candidate / planning_only 및 상세 사유 |
| 초보자 UI + 상세 설정 | 일상 관측 4단계 + 장비 프로필 관리 |

## v34.2에서 변경한 사용자 흐름

작품설명서에서는 전천 영상과 망원경 기준 영상을 하나의 분석 과정에 입력하는 형태로 서술되어 있습니다. v34.2는 그 **망원경 기준 영상의 물리적 역할은 유지하면서 입력 시점을 분리**했습니다.

### 장비 등록 시 1회
`망원경 기준 영상 → 배경/신호/PSF/포화/기기영점/Csys 보정 → 장비 프로필 저장`

### 관측 세션
`현재 전천 영상 + Stellarium 선택 천체 + 장비 프로필 → 권장 노출/장수/총 적분시간`

같은 장비 조합에서는 기준 영상으로 측정한 특성을 재사용할 수 있으므로, 매 관측마다 이미 알고 있는 장비 보정 자료를 다시 업로드하지 않도록 한 소프트웨어 구조 개선입니다.

## Csys와 기준 전천 영상

보고서의 Csys는 망원경 기준 배경과 같은 방향의 전천 기준 배경을 비교해야 합니다. 따라서 v34.2에서도 정량 Csys를 만들려면 장비 프로필 생성 시 **동일·유사 조건의 전천 기준 영상**을 함께 등록하는 것을 권장합니다.

그 자료가 없는 프로필은 사용할 수 있지만, 현재 전천 영상 내부의 방향별 상대 밝기를 저장된 망원경 기준 배경에 적용하는 planning-only 근사를 사용합니다. 이는 보고서가 지원하지 않는 정밀도를 임의로 주장하지 않기 위한 제한입니다.

## 좌표 보정 검증의 취급

작품설명서에 기록된 30개 기준별 회귀와 검사점 5 px 이하 오차는 방향별 배경 조회에 사용합니다. 그러나 5 px을 각도 RMS로 변환해 절대 입체각 측광 검증으로 가장하지 않습니다. 절대 표면광도 경로에는 별도의 angular RMS 자료가 필요합니다.

## 촬영시각 정합성

작품설명서의 대기질량·방향별 배경 조회는 천체의 Alt/Az가 실제 영상 촬영 시점과 대응한다는 전제가 필요합니다. v34.2는 장비 등록에서 기준 영상의 촬영시각을 먼저 읽고, 기준 천체의 RA/Dec와 관측 위치가 있으면 그 촬영시각의 Alt/Az를 Astropy로 다시 계산합니다. 따라서 Stellarium이 현재 날짜에 있어도 프로필 생성 자체는 중단하지 않습니다. 재계산에 필요한 좌표·위치·시간대가 부족한 경우에는 오래된 Alt/Az를 버리고 Csys·기준 대기질량처럼 시간 의존 보정만 비활성화합니다. 일반 EXIF의 시간대가 불명확한 경우 UTC로 임의 추정하지 않는 원칙은 유지합니다.


## 장비 기준 영상의 재사용 해석

보고서의 망원경 기준 영상은 실제 장비에서 신호·배경이 어떻게 기록되는지 측정하기 위한 자료입니다. v34.2는 이 측정을 장비 등록 시 한 번 수행해 저장하지만, **기준 사진 속 특정 시야의 가장 밝은 별까지 장비 고유 특성으로 재사용하지는 않습니다.** 과거 시야 대표별 포화시간은 진단값으로만 표시하고, 오늘 선택 대상이 점광원이면 카탈로그 광도와 프로필의 PSF peak fraction을 이용해 대상 포화 상한을 새로 계산합니다. 이는 “장비 특성의 재사용”과 “관측 장면의 재사용”을 구분하기 위한 수정입니다.
