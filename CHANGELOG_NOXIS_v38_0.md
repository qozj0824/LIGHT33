# NØXIS v38.0 변경사항

## 총 적분 계획
- 메인 세션에서 사용자 목표 SNR 기반 필요 장수 역산을 제거했습니다.
- 6개 밝기 구역별 SNR-시간 곡선과 구조 정보효용을 계산합니다.
- 빠름/균형/고품질/매우 깊게 4개 한계효용 기반 권고를 제공합니다.
- 기본 12시간의 유한 계획범위를 도입해 극미약 천체에서 비현실적인 무한 외삽을 막았습니다.
- 구조 신뢰도가 낮으면 평균 대상 신호 기반 효율 계산으로 fallback합니다.
- 신호/배경 불확실성을 추천 장수/적분시간 범위에 전파합니다.
- `stack_efficiency_curve.png`를 추가했습니다.

## 장비 프로필 보존
- 핵심 프로필 JSON과 미리보기 이미지를 브라우저 저장소에서 분리했습니다.
- 분석 요청에는 compact core snapshot만 전송합니다.
- Render-local 프로필 소실을 감지하면 자동으로 서버에 재생성합니다.
- JSON 내보내기/가져오기를 추가했습니다.
- 브라우저 저장 실패 상태를 UI에 표시합니다.
- 복원 profile id를 서버에서 검증해 경로 조작 입력을 거부합니다.

## 호환성
- 기존 API/테스트를 위해 `target_snr` 필드는 파싱할 수 있지만 현재 세션 권고에는 사용하지 않으며 `legacy_target_snr_ignored`로 기록합니다.
- 구형 `/api/analyze` 진단 경로는 기존 목표 SNR 모델을 유지합니다.
