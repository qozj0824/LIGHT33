# NØXIS UI refresh

- 상단을 NØXIS 워드마크만 남긴 로고 전용 바 형태로 정리
- N.I.N.A 계열의 조밀한 데스크톱 패널/사이드바 레이아웃 적용
- 장문의 안내, 서버 버전 상태, 기본 사용 흐름 등 비필수 문구 제거
- 전천 영상 / 관측 대상 / 촬영 조건 / 결과를 작업 패널 중심으로 재구성
- 장비 프로필 관리를 별도 모달 작업창으로 변경
- 결과 카드와 상세 진단 UI를 더 조밀하게 정리
- Render 배포에서도 Stellarium을 사용자 브라우저에서 직접 연결하도록 변경
- `/api/stellarium/normalize` 추가: 브라우저가 읽은 Stellarium 데이터를 서버에서 기존 파서로 정규화
- CSP에 localhost Stellarium 연결 허용 추가
- 기존 분석 계산, 프로필 계산, 결과 생성 로직은 유지

## UI / 연결 안정화 2차 수정

- 상단 워드마크를 순수 텍스트 `NØXIS`로 변경해 글자 겹침/깨짐 제거
- 장비 프로필 창에 신규 망원경 기준 영상과 전천 기준 영상 미리보기 추가
- 선택된 기존 장비 프로필의 등록 기준 영상 미리보기 추가
- 전천 파일 선택 즉시 브라우저 로컬 미리보기를 먼저 보여주고, 서버 미리보기는 백그라운드에서 보강
- `/api/inspect`에서 ADU 판정 또는 서버 미리보기 생성만 실패한 경우 파일 자체를 실패 처리하지 않도록 완화
- 대형 영상 미리보기 생성의 메모리 사용량 축소
- Stellarium 요청에 Chrome Local Network Access용 주소 공간 힌트 추가
- Stellarium CORS `*`와도 호환되도록 기본 요청을 credential-less 방식으로 변경하고 선택 암호 입력 지원
- 페이지 진입 시 Stellarium 연결을 자동 확인하고, 분석 실행 시 대상이 없으면 현재 선택 천체를 자동 가져오기
- 마지막 장비 프로필과 Stellarium 주소를 기억해 반복 입력 최소화
- 복잡한 장비 센서 설정과 Stellarium 세부 연결 설정을 접힌 고급 설정으로 이동
